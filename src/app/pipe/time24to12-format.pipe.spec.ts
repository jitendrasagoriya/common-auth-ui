import { Time24to12FormatPipe } from './time24to12-format.pipe';

describe('Time24to12FormatPipe', () => {
  it('create an instance', () => {
    const pipe = new Time24to12FormatPipe();
    expect(pipe).toBeTruthy();
  });
});
